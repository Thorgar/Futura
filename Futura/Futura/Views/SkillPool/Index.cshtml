@model IEnumerable<Futura.Models.SkillPool>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Erstellen", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.ActionLink("Entwicklername", "Index", new { sortOrder = ViewBag.NameSortParm })
        </th>
        <th>
            @Html.ActionLink("Produkt", "Index", new { sortOrder = ViewBag.NameSortParm })
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Entwickler.Enwicklername)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sprachen.Sprachtitel)
        </td>
        <td>
            @Html.ActionLink("Editieren", "Edit", new { id=item.SkillPoolID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SkillPoolID }) |
            @Html.ActionLink("Löschen", "Delete", new { id=item.SkillPoolID })
        </td>
    </tr>
}

</table>
