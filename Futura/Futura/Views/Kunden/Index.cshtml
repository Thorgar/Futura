@model IEnumerable<Futura.Models.Kunde>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kundenname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kundenname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KundenID }) |
            @Html.ActionLink("Details", "Details", new { id=item.KundenID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KundenID })
        </td>
    </tr>
}

</table>
